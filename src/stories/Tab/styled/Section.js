import { styled } from '@storybook/theming';

import { ELEMENT, FONTS } from '../../themes';

const Section = styled.section`
  font-size: ${FONTS.S.BASE};
  padding: ${ELEMENT.PADDING.Y} 0;
`;

export default Section;
